namespace Contracts.HistoricalPricing;

public sealed record HistoricalDocumentEstimateItemResponse(
    int ElementId,
    string? Reference,
    string? Category,
    string? System,
    string? Glass,
    string? Configuration,
    decimal? WidthMm,
    decimal? HeightMm,
    decimal? AreaM2,
    decimal? Quantity,
    string? Finish,
    string PricingStatus,
    decimal? UnitMinimum,
    decimal? UnitExpected,
    decimal? UnitMaximum,
    decimal? LineMinimum,
    decimal? LineExpected,
    decimal? LineMaximum,
    decimal? Minimum,
    decimal? Expected,
    decimal? Maximum,
    decimal? ConfidenceScore,
    string? ConfidenceLevel,
    bool RequiresReview,
    int? CandidateCount,
    int? StrongComparableCount,
    IReadOnlyList<string> MappingWarnings,
    IReadOnlyList<string> Assumptions,
    IReadOnlyList<string> MissingData);

public sealed record HistoricalDocumentEstimateResponse(
    Guid? ProjectId,
    Guid? RequirementId,
    int SourceCount,
    int ExtractedElementCount,
    int ItemCount,
    int PricedItemCount,
    int NotPriceableItemCount,
    int TechnicalFailureItemCount,
    string? Currency,
    string PricingBasis,
    decimal? Minimum,
    decimal? Expected,
    decimal? Maximum,
    decimal ConfidenceScore,
    string ConfidenceLevel,
    bool IsPartial,
    bool RequiresReview,
    IReadOnlyList<string> Assumptions,
    IReadOnlyList<string> MissingData,
    IReadOnlyList<string> Warnings,
    IReadOnlyList<HistoricalDocumentEstimateItemResponse> Items);
